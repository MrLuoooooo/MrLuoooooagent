# 2026-06-09 — Agent 应用四个维度的代码补齐

## 主题一：长期记忆系统
- 新建 `model/memory.go`、`store/memory.go`、`service/memory.go`
- `ESMemoryStore`：ES 向量检索 + 版本乐观锁 `Supersede` 解决冲突
- `MemoryService`：异步 `ExtractAndStore`（LLM提取）、`RetrieveRelevant`、`InjectIntoPrompt`
- 修改 `graph/agent.go`：`to_messages` 节点注入记忆块
- 修改 `handler/chat.go`：`SummarizeHistory` 摘要压缩 → 短期记忆不再是硬截断
- 修改 `config/types.go`：`MemoryConfig`（enabled + retrieval_top_k）
- 修改 `configs/config.yaml`：memory 段
- 修改 `fx.go`：加 ESMemoryStore + MemoryService provider

## 主题二：幻觉防御系统
- 新建 `model/feedback.go`、`service/feedback.go`、`store/feedback.go`、`handler/feedback.go`
- `FeedbackService.FactCheck`：工具结果 vs LLM 输出 → 识别幻觉
- `FeedbackService.CompareRAG`：源文档 vs 生成内容 → 检测 RAG 漂移
- `POST /api/v1/feedback`：用户点赞/点踩/纠错
- 重写 `service/approval.go`：JSON 文件持久化（`data/approvals.json`），重启不丢
- 修改 `fx.go` + `router.go`：注册反馈链

## 主题三：评估系统
- 新建 `eval/eval.go`：`CalculateRetrievalMetrics`（precision@k/recall@k/F1）、`RunAgentE2E`
- 新建 `eval/testdata/retrieval_labels.json`：5 条标注数据
- 新建 `eval/eval_test.go`：评估测试模板

## 主题四：置信度系统
- 新建 `service/confidence.go`：
  - 4 因子启发式打分：不确定语言检测、工具结果对齐、源文档对齐、输出质量
  - `SelfConsistency`：多次采样一致性（近似语义熵）
  - `InjectConfidenceNote`：低置信度时自动加提示
- 修改 `fx.go`：注册 ConfidenceService

## 测试状态
- `go build ./...` ✅ 通过
- 所有受影响包的测试 `-count=1` ✅ 通过
- 预存在的 `component/tool` 和 `handler/TestSkillRemove` 失败未引入

## 第二轮：串联合环 + 设计修复

### 闭环后处理（ChatService.Agent()）
- 新增 `confidenceSvc` 依赖注入到 ChatService
- 新增 `postProcess()` 方法：置信度评估 → 低置信标记 → 幻觉检测 → 记忆提取
- 流程：Agent 输出 → ES 存纯净版 → 置信度打分 → 如果 NeedVerify → FactCheck → InjectConfidenceNote → 异步提取记忆
- 修改 `fx.go`：ProvideChatService 加 confidenceSvc 参数
- 修改 `chat_test.go`：适配新签名（3处 nil → 4处 nil）

### 去重增强
- `memory.go`：FindByKeyword 从只看 Keywords[0] 改为遍历全部关键词
- 提高不同关键词排列下的冲突检测召回率

### 编译/测试
- `go build ./...` ✅ 
- `go test ./internal/{service,graph,server} -count=1` ✅

## 第三轮：异步化 + 防崩 + 跨实例共享

### SummarizeHistory 异步化
- `chat.go`：新增 `summaryCache sync.Map`，`SummarizeHistory` 签名改为 `(convID, messages, maxKeep) string`
  - 首次调用：返回截断占位 + 后台 goroutine 生成 LLM 摘要缓存
  - 下次调用：直接从缓存读取
- `handler/chat.go`：适配新签名，不再等 LLM 摘要

### ApprovalStore TTL
- 新增 `purgeExpired`：30 天 TTL，pending 项永不过期
- 每次 save() 时自动清理已处理的过期项
- 修复：CreatedAt 为零值时跳过清理（兼容测试和旧数据）

### 跨实例共享
- `SkillStore`、`ModelStore`、`ApprovalStore` 构造函数改为 `NewXxx(dataDir string)`
- Fx 层从 `cfg.Stock.DataDir` 读取，默认 `"data"`
- Docker/K8s 部署时挂载同一个 volume 到 `cfg.Stock.DataDir` → 多实例共享 JSON 文件
- 所有受影响的测试文件已更新
