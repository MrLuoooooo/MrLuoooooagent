# 2026-05-26

## 项目梳理：GoAgentPro 消息处理完整流程
- 从 POST /api/v1/chat 到 SSE 响应的 4 层处理链：HTTP 入口 → Service 编排 → Eino ReAct Agent 图 → SSE 流式输出
- Handler 三种分发：RAG 非流式、RAG 流式、Agent 模式（ReAct 循环 + 工具调用）
- ReAct 循环：to_messages → chat_model → parse_tool_calls → 分支（tools→tool_as_user→chat_model 或 END）

## 简历项目评分（面试官视角）
- 总分 73/100：架构 24、代码质量 16、技术深度 14、完成度 12、创新 7
- 28 个测试文件覆盖核心包，httptest + mock 设计扎实
- 扣分项：baseSystemPrompt 硬编码、toWindowsPath 只处理 D 盘、全局单例工具注册表

## 配置化重构
- 在 config/types.go 新增 AgentConfig（含 system_prompt 字段）
- 在 configs/config.yaml 新增 agent.system_prompt 配置段
- 修改 graph/agent.go：NewAgentGraph 接受 systemPrompt 参数，删除硬编码 const
- 修改 server/fx.go：ProvideAgentGraph 从 cfg.Agent.SystemPrompt 读取
- 修复 handler/chat.go：toWindowsPath 支持任意盘符（A-Z/a-z），新增 isDriveLetter 辅助函数
- agent_test.go 同步更新，添加 "test prompt" 参数
- 全量编译 + 相关测试通过（已有的 tool/fs_test 路径问题和 graph/tool_parser 测试问题与本次改动无关）

## System Prompt 重写
- 将原 250 字单行 prompt 扩展为结构化的 YAML 多行块（|），约 1500 字
- 分 7 个模块：核心原则 / 文件操作 / 命令执行 / 联网搜索 / 股票查询 / 知识库 / 不需要工具时的行为 / 错误处理
- 新增规则：工具失败自动重试（最多 2 次）、editing 优先精准替换、Git Bash 路径规范、破坏性命令防护、RAG 结果综合判断

## 代码审查：GoAgentPro 面试官视角评分
- 总分 68/100（架构 78, 代码质量 65, 安全性 42, 可测试性 55, 可维护性 72, 工程化 75, 文档 70）
- P0 致命问题：API Key 硬编码 config.yaml、模块路径 yourusername 占位、resolveModelProvider 返回 nil 无保护、BatchTool atomic.Value 类型断言可能 panic
- P1 严重问题：全局单例（globalRegistry/batchToolInstance）、searchByContent O(n²) 全文扫描、tool_parser ID 生成 >9 崩溃、assistant 消息双写 bug、agent.go 缩进错乱
- P2 中等问题：ApprovalStore 纯内存无持久化、无结构化错误类型、SSE header 后 error 语义混乱、ES 无重试配置、Config.Load 吞错误、硬编码 Windows 路径、冒泡排序
- 亮点：分层架构清晰、fx 依赖注入、安全命令检查 tokenize 方法、RAG 三级过滤、Docker 编排完整、ModelManager 热切换、cron 审批流程

## 生成专业提示词规则
- 将代码审查中发现的 21 条问题提炼为结构化规则文件 `.workbuddy/prompts/code-review-rules.md`
- 分类：安全规则(P0, R1-R3) / 健壮性(P1, R4-R10) / 工程质量(P2, R11-R18) / 代码风格(R19-R21)
- 每条规则含：触发条件、违规示例(❌)、正确做法(✅)
- 末尾附合并前检查清单

## 面试官视角：GoAgentPro 项目终评（2026-05-26 第二轮）
- **总分 74/100，B+**。架构 80、工程实践 68、代码质量 72、测试 65、功能 82、运维 78、文档 75
- 统计：~11,800 行源码 + ~3,500 行测试，21 个测试包中 14 个通过（67%），4 个失败
- 上一轮 P0 问题已修复：WebSearch 支持 3 种后端、Document 已迁移 ES、Auth 正确阻断、List/Delete 对接真实服务
- 架构亮点：Clean Architecture 四层分明、fx DI 规范、Eino Graph/Chain 使用正确、19+ 工具覆盖广
- 新发现问题：模块路径 `MrLuoooooo/MrLuoooooagent` 不专业、go.mod 声明 `go 1.25`（不存在的版本）、server.exe 和 bash.exe.stackdump 进入版本控制、4 个临时文件（.177977* 后缀）、context.Background() 滥用（SaveMessages 丢弃调用方 context）、json.Marshal 吞错误（esindexer.go:100-110）
- 测试扣分：4 个包测试失败（scheduler/tool/config/server），文件系统测试在 Windows 环境下失败说明未做跨平台测试
- 结论：自学能力强，架构理解到位，但工程纪律不够——"写完就算"的习惯需要在有 code review 文化的团队中打磨

## 项目清理
- 两轮扫描删除垃圾：server.exe(60MB) + tool.test.exe(22MB) + bash.exe.stackdump + 16个编辑器临时文件 + 空目录 tmp/、tool/tmp、.vite-temp
- .gitignore 补充 `*.tmp.*` 规则防止编辑器临时文件再被提交

## 注释去 AI 味
- 全项目 80+ 文件注释改写：去掉 "implements X interface"、"creates a Y"、"handles POST"、"returns all"、"The caller is responsible" 等 AI 生成式注释
- 改为自然中文：结构体→功能描述、构造函数→"NewXxx —"、方法→一句话说清做什么
- 覆盖范围：handler/、service/、graph/、pipeline/、component/、store/、config/、middleware/、scheduler/ 全部子包
- 最终验证：AI 味模式 0 残留，编译通过，handler 测试通过
