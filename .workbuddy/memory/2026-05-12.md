# 2026-05-12

## Pregel 分析
- 深入分析了 ChatModelAgent 的 ReAct 循环与 Eino Pregel 的关系
- 确认 `internal/graph/agent.go` 的 `NewAgentGraph` 构建的含环图默认使用 Pregel 模式
- 关键证据：`compose/graph.go:645` 默认 `runType = runTypePregel`，Pregel 允许有环图且实现超步循环引擎
- 14 步循环上限（`maxRunSteps = len(chanSubscribeTo) + 10 = 4 + 10`）防止死循环
- `compose/graph_run.go:241` 的 `for step := 0; ; step++` 是 Pregel 核心执行循环
